version: '3.8'

services:
  network-provider:
    build: .
    ports:
      - "8004:8000"
    environment:
      - ENVIRONMENT=development
      - LOG_LEVEL=info
    volumes:
      - ./src:/app/src
    command: python -m uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload
